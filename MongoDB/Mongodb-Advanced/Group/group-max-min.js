// group by sum , avg, min, max

// group na kore , sob golo data er opore minimum, maximum , avg, sum ber hobe.


// aggregate use
//db.products.aggregate([
    // {$group:{_id:0,min:{$min:"$salary"}}}
// ])

//db.products.aggregate([
    // {$group:{_id:0,max:{$max:"$salary"}}}
// ])

//db.products.aggregate([
    // {$group:{_id:0,sum:{$sum:"$salary"}}}
// ])

//db.products.aggregate([
    // {$group:{_id:0,avg:{$avg:"$salary"}}}
// ])



//without groping tar jonno _id:0 hobe